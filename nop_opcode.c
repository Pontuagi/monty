#include "monty"

/**
* nop - doesn't do anything
* @stack: stack
* @line_number: number of line
*
* Return: Void
*/
void nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
